# Handoff — 2026-03-30 — ScrollView 子节点布局不对称（按钮右边空白大于左边）

## 1. 当前任务目标
修复 react-solar2d 框架中 ScrollView 内子节点（按钮、卡片等）右边空白明显大于左边的布局 bug。用户在模拟器和手机上都确认了这个问题。

## 2. 当前进展

### 已提交 (commit 6818697)
- Image borderRadius 修复（roundedRect + image fill）
- 根目录 main.lua/config.lua/build.settings + Makefile
- yoga 插件改为 GitHub release 下载
- DemoPage 去掉了固定 width（之前用 `display.contentWidth - 24`）

### 未提交的改动（当前工作区）
- `renderer/HostConfig.lua` — `_pendingBg` 延迟创建机制（vw==0 || vh==0 时不创建 _bg，存 _pendingBg）
- `renderer/init.lua` — applyLayout 中处理 `_pendingBg`（Yoga 算出 w/h 后首次创建 _bg）+ 0x0 rect recreate 逻辑
- `examples/kitchen_sink/theme.lua` — DemoPage 有 `backgroundColor = T.bg`（debug 用的，需要清理）
- 一些测试文件适配了新的 _pendingBg 行为

### 关键确认
- **`_pendingBg` 机制已生效** — debug print 确认 Button 的 _bg 在 applyLayout 中以正确尺寸（w=319 h=40）创建
- **Yoga 计算完全正确** — ScrollView w=351, 内容 View w=351, Button w=319 (351-32 padding)
- **但视觉上按钮仍然右边空白大于左边** — 模拟器截图和手机截图都一样

## 3. 关键上下文

### 测试方法（必须用这个，不要自己发明）
```bash
# 启动模拟器
echo "sink" > .route
/Applications/Corona-b3/Corona\ Simulator.app/Contents/MacOS/Corona\ Simulator \
  -project /Users/yee/data/dev/app/react-solar2d/main.lua -no-console YES &
sleep 6

# 导航到 Toast 页面
curl -s -X POST 'http://localhost:9876/navigate' -d 'route=Toast'
sleep 2

# 截图并直接用 Read 工具看（多模态！不要用 Python PIL 量像素！）
curl -s --max-time 15 'http://localhost:9876/screenshot' -o /tmp/shot.json
python3 -c "import json,base64; d=json.load(open('/tmp/shot.json')); open('/tmp/shot.png','wb').write(base64.b64decode(d['base64']))"
# Read /tmp/shot.png
```

### 关键文件
- `renderer/init.lua` — buildLayoutTree (28-83), applyLayout (88-300)
- `renderer/HostConfig.lua` — createInstance View 部分 (413-456), _pendingBg
- `renderer/ScrollViewFactory.lua` — ScrollView 创建，默认 w=display.contentWidth
- `components/Button.lua` — backgroundColor + borderRadius=4, 没有显式 width
- `examples/kitchen_sink/theme.lua` — DemoPage, Section
- `examples/KitchenSinkApp.lua` — 外层容器 paddingHorizontal=12

### 关键数据（模拟器）
- config.lua: width=1536, height=2048, scale="adaptive"
- 模拟器报告: contentWidth=375, actualContentWidth=375
- Toast ScrollView: Yoga w=351 (375-24)
- DemoPage 内层 View: Yoga w=351
- Button: Yoga w=319 (351-32)
- **_pendingBg 创建确认**: Button _bg 以 w=319 h=40 创建

## 4. 关键发现

1. **Yoga 计算 100% 正确** — 反复验证，LuaJIT 独立测试 + 模拟器 debug print
2. **_pendingBg 延迟创建机制已生效** — _bg 以正确尺寸创建
3. **但视觉效果不变** — _bg 尺寸正确但按钮视觉宽度没变
4. **问题可能不在 _bg 尺寸** — _bg 是背景矩形，但 Solar2D group 的视觉边界由所有子元素决定
5. **之前测量说"左右 86px 对称"是错误的判断** — 用户多次确认右边空白明显更大

## 5. 未完成事项

1. **【最高优先级】找到视觉不对称的真正原因** — _bg 尺寸对了但视觉不对，问题在哪？
   - 可能是 ScrollView 的 touchOverlay 或 contentGroup 偏移？
   - 可能是 screenOriginX 非零导致内容偏移？
   - 可能是 display object 的 x 坐标计算有误？
2. 清理 debug 代码（theme.lua 的 backgroundColor=T.bg）
3. 清理 .route 文件
4. 跑 lua run_tests.lua 确认无回归
5. 提交推送

## 6. 建议接手路径

### 新方向：不要再查 _bg 尺寸了
_bg 尺寸已确认正确。问题在**别的地方**。建议：

1. **给按钮加明亮的边框**（如 `borderWidth=2, borderColor="#FF0000"`）看按钮实际的 display object 边界在哪
2. **检查 ScrollView contentGroup 的 x 坐标** — 可能 contentGroup 有偏移
3. **检查 screenOriginX** — adaptive 缩放可能导致 screenOriginX 非零
4. **在 applyLayout 里 print 按钮的实际 display object x, y 坐标**
5. **对比 "← Toast" 行的宽度和按钮的宽度** — "← Toast" 看起来满宽，按钮不满宽，它们在不同的容器里吗？

### 可能的新根因假设
- ScrollView 的 contentGroup 可能被 touchOverlay 影响了位置
- KitchenSinkApp 的外层 View 可能有不对称的 padding/margin
- `applyLayout` 中 x 坐标的计算可能有 off-by-one

## 7. 风险与注意事项

- **不要再纠结 _bg 尺寸** — 已确认正确，问题不在这里
- **不要用 Python PIL 量像素说"对称"** — 用户视觉确认不对称，以用户为准
- **直接用 Read 工具看截图** — 多模态模型能看图
- 当前有未提交的改动，`git stash` 或提交前注意
- renderer/init.lua 和 HostConfig.lua 都有改动
- theme.lua 有 debug backgroundColor 需要清理

## 下一位 Agent 的第一步建议
> 1. 启动模拟器导航到 Toast 页面（用上面的方法）
> 2. 截图用 Read 看，确认问题存在
> 3. 在 DemoPage 的 View 上加 `borderWidth = 2, borderColor = "#FF0000"`，截图看红色边框的位置——确认 View 的 display object 实际位置和尺寸
> 4. 重点关注：红色边框是否左右对称？如果不对称，问题在容器定位；如果对称但按钮内容不对称，问题在按钮内部
